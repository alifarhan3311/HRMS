/**
 * features/employees/validation/employees.validation.js
 * Yup schema used by the employees feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const employeesSchema = yup.object({
  // fields populated during UI build-out for employees
});
