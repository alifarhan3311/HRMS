/**
 * features/payroll/validation/payroll.validation.js
 * Yup schema used by the payroll feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const payrollSchema = yup.object({
  // fields populated during UI build-out for payroll
});
