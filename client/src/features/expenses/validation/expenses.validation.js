/**
 * features/expenses/validation/expenses.validation.js
 * Yup schema used by the expenses feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const expensesSchema = yup.object({
  // fields populated during UI build-out for expenses
});
