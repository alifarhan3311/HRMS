/**
 * features/dashboard/validation/dashboard.validation.js
 * Yup schema used by the dashboard feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const dashboardSchema = yup.object({
  // fields populated during UI build-out for dashboard
});
