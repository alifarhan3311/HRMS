/**
 * features/attendance/validation/attendance.validation.js
 * Yup schema used by the attendance feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const attendanceSchema = yup.object({
  // fields populated during UI build-out for attendance
});
