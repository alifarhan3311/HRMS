/**
 * features/leaves/validation/leaves.validation.js
 * Yup schema used by the leaves feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const leavesSchema = yup.object({
  // fields populated during UI build-out for leaves
});
