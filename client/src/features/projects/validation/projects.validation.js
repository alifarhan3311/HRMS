/**
 * features/projects/validation/projects.validation.js
 * Yup schema used by the projects feature's create/edit forms.
 * Field-specific rules get filled in as each form is built out.
 */
import * as yup from 'yup';

export const projectsSchema = yup.object({
  // fields populated during UI build-out for projects
});
